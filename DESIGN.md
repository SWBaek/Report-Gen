# Report-Gen Design System

- 상태: **v1 기준 문서**
- 적용 범위: 정적 HTML 보고서 렌더러와 그 결과물
- 브랜드 근거 최종 확인: **2026-09-01**

이 문서는 LG전자의 공개 브랜드 아이덴티티를 Report-Gen의 일관된 보고서 UI로 번역한 구현 계약이다. 공식 LG 자산과 지침이 이 문서와 충돌하면 공식 자산 및 최신 승인 지침이 우선한다.

## 1. 설계 목표

Report-Gen은 주제나 데이터 양이 달라도 같은 정보 구조와 시각적 위계를 유지해야 한다.

1. **따뜻한 정밀함** — LG의 Emotionally Intelligent Design처럼 인간적인 온기와 기술적 명료함을 함께 전달한다.
2. **내용 우선** — 브랜드 표현은 분석 결과를 돋보이게 해야 하며, 장식이 정보보다 앞서지 않는다.
3. **Borderless** — 종이 모양의 외곽 프레임이나 카드 테두리에 의존하지 않는다. 여백, 배경색, 크기, 정렬로 구획한다.
4. **결정적 렌더링** — 동일한 구조화 데이터와 디자인 버전은 동일한 시각 구조를 만든다. LLM이 임의의 레이아웃이나 CSS를 생성하지 않는다.
5. **내용 탄력성** — 짧은 문장, 장문, 긴 URL, 끊기지 않는 문자열, 표와 다국어 텍스트가 레이아웃을 깨뜨리지 않는다.
6. **절제된 브랜드 사용** — 빨강은 식별과 강조에 집중하고, 본문과 데이터 영역은 중립색으로 읽기 쉽게 유지한다.

## 2. 공식 LG 아이덴티티 근거

다음 항목은 LG전자 공식 공개 자료에서 확인한 브랜드 사실이다.

| 요소 | 공식 기준 | Report-Gen 채택 방식 |
| --- | --- | --- |
| 철학 | Emotionally Intelligent Design은 따뜻함과 혁신을 결합한다. | 친근한 곡선, 명확한 정보 구조, 낙관적이되 과장하지 않는 표현을 사용한다. |
| 로고 | 원형 얼굴 심벌과 워드마크가 기본이며, 밝은 배경의 Heritage Red 버전이 주 식별 자산이다. | 승인된 원본 자산만 사용한다. 로고를 CSS, 텍스트, 임의 SVG로 다시 그리지 않는다. |
| 핵심 색 | Active Red, Heritage Red, Warm Grey, White, Black이 공식 팔레트다. | 아래의 원색 토큰은 값을 변경하지 않는다. |
| 서체 | LG EI Headline은 고강도 제목용, LG EI Text는 본문·UI 가독성용이다. | 정식 폰트 파일과 사용 권한이 있을 때 우선 사용하고, 없으면 시스템 폰트로 안전하게 대체한다. |
| 형태 | 원, 사각형, 스타디움, 초승달 등 LG 제품의 둥근 형태가 EI 시스템의 기반이다. | 둥근 섹션 배경과 원형 포커스 요소를 제한적으로 사용한다. |
| 슬로건 | `Life's Good.`은 리드 메시지 또는 마무리 서명으로 사용하며 Active Red, Black, White 색상형이 있다. | 표지 또는 마지막 서명 중 한 곳에서만 선택적으로 사용한다. 본문 장식 문구로 반복하지 않는다. |
| 보이스 | Smile, Insight, Design의 세 원칙으로 따뜻하고 유용하며 읽기 쉬운 문장을 만든다. | 짧고 스캔 가능한 문장, 사람에게 의미 있는 해석, 명확한 결론을 우선한다. |

공식 근거: [Our Identity](https://www.lg.com/global/our-identity/), [Logo](https://www.lg.com/global/our-identity/logo/), [Color](https://www.lg.com/global/our-identity/color/), [Typography](https://www.lg.com/global/our-identity/typography/), [Design System](https://www.lg.com/global/our-identity/design-system/), [Slogan](https://www.lg.com/global/our-identity/slogan/), [Voice](https://www.lg.com/global/our-identity/voice/).

## 3. 색상 시스템

### 3.1 공식 원색 토큰

| 토큰 | 값 | 역할 |
| --- | --- | --- |
| `--lg-active-red` | `#FD312E` | 디지털 에너지, 핵심 강조, 포커스 데이터 |
| `--lg-heritage-red` | `#A50034` | 로고, 링크, 깊은 브랜드 배경, 접근 가능한 강조 텍스트 |
| `--lg-warm-grey` | `#F0ECE4` | 부드러운 섹션 배경, 표 헤더, 보조 면 |
| `--lg-white` | `#FFFFFF` | 기본 배경과 어두운 면 위 텍스트 |
| `--lg-black` | `#000000` | 본문, 제목, 데이터, 강한 대비 |

이 다섯 값은 공식 LG 색상과 동일하게 유지한다. 임의로 비슷한 빨강이나 차가운 회색으로 대체하지 않는다.

### 3.2 의미 토큰

```css
:root {
  --color-page: #FFFFFF;
  --color-surface: #F0ECE4;
  --color-ink: #000000;
  --color-ink-muted: rgb(0 0 0 / 68%);
  --color-brand-active: #FD312E;
  --color-brand-deep: #A50034;
  --color-on-deep: #FFFFFF;
  --color-focus: #A50034;
  --color-rule: rgb(0 0 0 / 14%);
}
```

- 기본 본문은 Black on White다.
- Warm Grey는 카드 테두리 대신 섹션 면을 구분할 때 사용한다.
- 일반 크기 링크와 강조 텍스트는 Heritage Red를 사용한다.
- Active Red는 핵심 수치, 큰 제목, 아이콘, 차트의 한 개 포커스 시리즈처럼 제한된 영역에 사용한다.
- Active Red를 오류 의미로 고정하지 않는다. 브랜드 강조와 상태 의미를 혼동하지 않는다.
- 상태색이나 다중 계열 차트 팔레트가 필요해지면 접근성 검증을 거쳐 별도 의미 토큰으로 추가한다.

### 3.3 대비 규칙

WCAG 2.2 AA를 최소 기준으로 삼는다. 일반 텍스트는 `4.5:1`, 큰 텍스트와 핵심 비문자 UI는 `3:1` 이상이어야 한다.

| 조합 | 대비 | 사용 판정 |
| --- | ---: | --- |
| Black / White | `21.00:1` | 모든 텍스트에 사용 가능 |
| Black / Warm Grey | `18.31:1` | 모든 텍스트에 사용 가능 |
| Heritage Red / White | `7.07:1` | 일반 텍스트와 링크에 사용 가능 |
| Heritage Red / Warm Grey | `6.17:1` | 일반 텍스트에 사용 가능 |
| Black / Active Red | `6.05:1` | 일반 텍스트에 사용 가능 |
| White / Active Red | `3.47:1` | 큰 제목 또는 로고에만 사용; 일반 본문 금지 |
| Active Red / White | `3.47:1` | 큰 텍스트와 비문자 강조만 허용; 일반 링크 금지 |

그라디언트나 이미지 위 텍스트는 가장 낮은 대비 지점을 기준으로 검사한다. 확인할 수 없으면 단색 배경을 사용한다.

## 4. 타이포그래피

### 4.1 폰트 스택

LG EI 폰트는 LG전자의 전용 브랜드 서체다. 공식 파일과 프로젝트 사용 권한이 확인되기 전에는 저장소에 복사하거나 외부 URL로 불러오지 않는다.

```css
:root {
  --font-display: "LG EI Headline", "LG EI Text", system-ui, -apple-system,
    "Segoe UI", "Apple SD Gothic Neo", "Malgun Gothic", sans-serif;
  --font-body: "LG EI Text", system-ui, -apple-system, "Segoe UI",
    "Apple SD Gothic Neo", "Malgun Gothic", sans-serif;
}
```

- 표지 제목과 대표 수치: `--font-display`
- 본문, 표, 라벨, 캡션: `--font-body`
- 숫자 비교가 중요한 표와 KPI에는 `font-variant-numeric: tabular-nums`를 적용한다.
- 본문에 강제 대문자 변환을 사용하지 않는다. 한국어 자간은 기본값을 우선한다.

### 4.2 타입 스케일

| 역할 | 크기 | 행간 | 굵기 | 사용 |
| --- | --- | --- | --- | --- |
| Display | `clamp(36px, 5vw, 72px)` | `1.02` | 700 | 표지의 짧은 핵심 메시지 |
| Report title | `clamp(32px, 4vw, 56px)` | `1.10` | 700 | 보고서 제목 |
| Section title | `clamp(24px, 2.5vw, 32px)` | `1.20` | 650 | 대단원 |
| Subsection title | `20px` | `1.35` | 650 | 소단원과 카드 제목 |
| Body | `16px` | `1.65` | 400 | 본문과 설명 |
| Data label | `13px` | `1.45` | 600 | 단위, 라벨, 메타데이터 |
| Caption | `12px` | `1.50` | 400 | 출처, 주석, 보조 설명 |

- 긴 본문은 최대 `68ch`로 제한한다.
- 본문은 왼쪽 정렬을 기본으로 한다. 여러 줄 본문을 중앙 정렬하지 않는다.
- 한 화면에 Display 크기는 한 번만 사용한다.
- 제목 레벨을 시각 크기만으로 흉내 내지 말고 `h1`부터 순서대로 사용한다.

## 5. 공간과 형태

### 5.1 간격 토큰

`4, 8, 12, 16, 24, 32, 48, 64, 96px`의 고정 스케일을 사용한다.

```css
:root {
  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-6: 24px;
  --space-8: 32px;
  --space-12: 48px;
  --space-16: 64px;
  --space-24: 96px;
  --radius-sm: 12px;
  --radius-lg: 24px;
  --radius-pill: 999px;
}
```

- 섹션 간격은 데스크톱 `64–96px`, 모바일 `40–64px`를 기본으로 한다.
- 관련 요소는 가까이, 다른 주제는 충분히 멀리 배치한다.
- 기본 카드와 섹션은 그림자보다 Warm Grey 면과 여백으로 구분한다.
- 둥근 모서리는 LG의 제품 기반 EI 형태를 반영하되 모든 요소를 알약 모양으로 만들지 않는다.

### 5.2 Borderless의 의미

- 페이지 가장자리를 종이 프레임이나 외곽선으로 감싸지 않는다.
- 모든 카드를 1px 사각 테두리로 나누지 않는다.
- 데이터 표의 가독성을 위한 얇은 행 구분선과 포커스 표시선은 허용한다.
- 섹션 구분은 우선순위대로 `여백 → 배경 면 → 타이포 위계 → 최소 구분선`을 사용한다.

## 6. 반응형 레이아웃

```css
:root {
  --content-max: 1200px;
  --page-gutter: clamp(16px, 4vw, 64px);
}
```

- 보고서 배경은 뷰포트를 채우며, 본문 컨테이너는 최대 `1200px`다.
- 좌우 여백은 `--page-gutter`를 사용한다.
- `1024px` 이상은 12열, `640–1023px`는 6열, `639px` 이하는 4열을 개념적 기준으로 사용한다.
- 실제 컴포넌트는 고정 열 수보다 `auto-fit`과 안전한 최소 너비를 우선한다.
- `320 CSS px` 폭에서도 핵심 내용이 한 열로 재배치되어야 한다.
- 페이지 전체의 가로 스크롤은 허용하지 않는다. 넓은 표·코드·다이어그램만 해당 컴포넌트 내부에서 가로 스크롤할 수 있다.

## 7. 고정 보고서 구조

렌더러는 아래 순서를 소유한다. LLM은 허용된 필드의 내용을 채우며 임의 섹션 마크업을 만들지 않는다.

1. **Brand header** — 승인된 로고, 보고서 유형, 생성 메타데이터
2. **Hero** — 제목, 한 문장 목적, 기준일 또는 분석 범위
3. **Executive summary** — 핵심 결론과 주요 시사점
4. **Key metrics** — 비교 가능한 대표 수치
5. **Analysis sections** — 구조화된 제목, 설명, 근거, 시각화
6. **Conclusion / actions** — 결론, 권고 또는 후속 조치
7. **Sources / footer** — 출처, 방법론 주석, 생성 정보, 선택적 슬로건

- 선택 섹션에 데이터가 없으면 섹션 전체를 제거하고 빈 카드나 큰 공백을 남기지 않는다.
- 필수 데이터가 없으면 값을 추정하지 말고 `정보 없음` 또는 해당 로케일의 명시적 결측 라벨을 표시한다.
- 섹션 순서와 컴포넌트 종류는 스키마와 렌더러가 결정한다.

## 8. 컴포넌트 계약

### Brand header

- 로고는 좌측 상단을 기본 위치로 하고 보고서 콘텐츠보다 먼저 인식되되 과도하게 크지 않게 한다.
- 공식 로고 자산의 비율, 색상, 안전 여백을 유지한다.
- 로고를 찌그러뜨리거나 회전하고, 그림자·외곽선·임의 색상을 추가하지 않는다.
- 보고서에서 심벌 높이는 최소 `24px`로 하되, 공식 자산 지침이 더 큰 값을 요구하면 그 지침을 따른다.

### Hero

- 제목, 목적, 범위만 배치한다. 핵심 메시지는 최대 2–3줄을 목표로 하되 필수 내용을 잘라내지 않는다.
- 브랜드 그라디언트는 Hero 또는 마지막 서명 영역 중 한 곳에서만 사용한다.
- 이미지나 그라디언트 위 본문을 직접 놓지 않는다. 충분한 대비의 단색 면이나 검증된 오버레이를 사용한다.

### Executive summary

- 본문보다 한 단계 큰 텍스트와 넓은 여백을 사용한다.
- 3–5개의 핵심 포인트를 권장하되 데이터가 적으면 억지로 개수를 채우지 않는다.
- 문장마다 결론과 근거를 분리해 빠르게 스캔할 수 있게 한다.

### Metric block

- 값 → 단위 → 라벨 → 비교 기준 순으로 읽힌다.
- 대표 수치만 Active Red를 사용할 수 있다. 모든 숫자를 빨강으로 만들지 않는다.
- 긴 단위와 라벨은 줄바꿈하며 고정 높이를 사용하지 않는다.
- 값이 없거나 계산 불가능하면 `0`으로 대체하지 않는다.

### Narrative section

- 제목, 요약 문장, 세부 근거 순으로 구성한다.
- 한 문단은 하나의 주장에 집중한다.
- 본문 폭은 `68ch`를 넘기지 않으며, 시각 자료와 본문은 작은 화면에서 한 열로 쌓인다.

### Callout

- Warm Grey 면과 `--radius-lg`를 기본으로 하고, 필요한 경우 Active Red의 짧은 시각 표지만 추가한다.
- 중요한 결론, 주의, 방법론 메모를 구분하지만 색만으로 의미를 전달하지 않는다.
- 본문을 복제해 강조하지 않는다.

### Data table

- 실제 `table`, `thead`, `tbody`, `th` 구조와 적절한 헤더 범위를 사용한다.
- 헤더는 Warm Grey 면, 데이터는 Black on White를 기본으로 한다.
- 셀 전체를 둘러싼 격자 대신 최소한의 가로 구분선을 사용한다.
- 숫자는 우측 정렬, 텍스트는 좌측 정렬한다.
- 넓은 표는 자체 컨테이너에서 스크롤하고 페이지 폭을 밀어내지 않는다.
- 모바일에서 숨긴 열 때문에 의미가 달라지면 열을 제거하지 말고 스크롤 또는 행별 재배치를 사용한다.

### Chart

- Active Red는 하나의 포커스 시리즈에만 사용한다. 기준선과 비교 시리즈는 Black 계열과 명시적 라벨을 사용한다.
- 색만으로 계열을 구분하지 않는다. 직접 라벨, 선 모양, 점 모양 또는 패턴을 함께 사용한다.
- 축, 단위, 기준 시점, 데이터 출처를 생략하지 않는다.
- 3D 차트, 장식용 원근, 의미 없는 그라디언트를 사용하지 않는다.
- 데이터가 표로 더 명확하면 차트 대신 표를 사용한다.

### Sources and footer

- 출처 제목, URL 또는 문서 식별자, 기준일을 가능한 범위에서 표시한다.
- 긴 URL은 줄바꿈 가능해야 하며 레이아웃을 넓히지 않는다.
- `Life's Good.`은 공식 자산을 사용할 수 있을 때만 선택적 서명으로 배치한다. 비슷한 폰트로 슬로건 로고를 재현하지 않는다.

## 9. 콘텐츠와 보이스

- **Smile** — 차갑거나 위협적인 문장보다 차분하고 사람 중심적인 문장을 사용한다.
- **Insight** — 수치 나열에서 멈추지 말고 무엇이 달라졌고 왜 중요한지 설명한다.
- **Design** — 짧은 문장, 명확한 제목, 스캔 가능한 목록으로 읽기 리듬을 만든다.
- 낙관성은 사실을 가리거나 위험을 축소하는 표현이 아니다. 부정적 결과도 직접적이고 존중하는 언어로 전달한다.
- 마케팅 과장, 근거 없는 최상급, 느낌표 남용을 피한다.
- 제목은 문장형을 기본으로 하며, 입력 데이터의 고유 표기와 약어를 보존한다.
- 보고서의 로케일을 일관되게 유지하고, 한국어·영어 혼합 시 단위와 날짜 표기를 통일한다.

## 10. 오버플로 및 콘텐츠 안전

다음 규칙은 구현 필수 조건이다.

```css
*, *::before, *::after { box-sizing: border-box; }
img, svg, video, canvas { max-width: 100%; height: auto; }
main, section, article, div, li, td, th { min-width: 0; }
p, li, td, th, a { overflow-wrap: anywhere; }
pre, .table-scroll, .diagram-scroll { max-width: 100%; overflow-x: auto; }
```

- 데이터 기반 영역에 고정 높이를 사용하지 않는다.
- 필수 내용에 `line-clamp`, 말줄임표 또는 `overflow: hidden`을 사용하지 않는다.
- `body { overflow-x: hidden; }`로 결함을 숨기지 않는다. 원인이 되는 너비와 최소 크기를 수정한다.
- 긴 문자열을 포함하는 Grid/Flex 자식에는 `min-width: 0`을 적용한다.
- 미디어에는 원본 비율을 유지하는 너비 제약을 둔다.
- LLM과 사용자 텍스트를 원시 HTML로 삽입하지 않는다. 구조화 필드를 이스케이프된 텍스트와 허용된 컴포넌트로 렌더링한다.
- 외부 이미지, 폰트, CSS를 실행 시점에 핫링크하지 않는다. 승인된 자산은 로컬 번들 또는 자체 포함 방식으로 제공한다.

## 11. 접근성

- 목표 수준은 [WCAG 2.2 AA](https://www.w3.org/TR/WCAG22/)다.
- 텍스트 확대 `200%`에서 내용이나 기능이 손실되지 않아야 한다.
- `320 CSS px` 리플로우에서 표·코드처럼 본질적으로 2차원인 영역 외에는 양방향 스크롤이 없어야 한다.
- 제목 순서, 목록, 표, 인용, 링크를 의미에 맞는 HTML로 작성한다.
- 의미 있는 이미지에는 대체 텍스트를 제공하고 장식 이미지는 빈 대체 텍스트를 사용한다.
- 상호작용이 추가되면 키보드 접근, 보이는 포커스, 충분한 클릭 영역을 제공한다.
- 모션이 추가되면 `prefers-reduced-motion`에서 정지 상태를 제공한다. v1 정적 보고서는 정보 전달에 모션을 요구하지 않는다.

## 12. 검수 기준

렌더러 변경은 최소한 다음 조합으로 확인한다.

### 뷰포트

- `320px` — 최소 리플로우
- `768px` — 태블릿
- `1280px` — 기본 데스크톱
- `1440px` 이상 — 넓은 화면에서 최대 너비와 여백

### 콘텐츠 스트레스 사례

- 제목과 본문이 매우 짧은 보고서
- 여러 문단, 목록, 주석이 포함된 장문 보고서
- 200자 이상의 공백 없는 문자열과 긴 URL
- 한국어, 영어, 숫자, 단위가 혼합된 내용
- 매우 큰 값, 음수, 소수, 결측값
- 열이 많은 표와 행이 많은 표
- 이미지 없음, 이미지 로딩 실패, 극단적인 가로·세로 비율 이미지
- 선택 섹션이 일부 또는 전부 비어 있는 입력

### 합격 조건

- 페이지 전체에 의도하지 않은 가로 스크롤이 없다.
- 텍스트 잘림, 겹침, 컨테이너 이탈, 빈 고정 높이 영역이 없다.
- 필수 내용이 말줄임표나 접힘으로 사라지지 않는다.
- 색 대비와 제목 순서가 기준을 충족한다.
- 같은 구조화 입력은 같은 컴포넌트 순서와 시각 위계를 만든다.
- 로고와 슬로건은 승인된 자산으로만 렌더링된다.
- 표와 차트는 제목, 단위, 기준, 출처를 유지한다.

## 13. 향후 출력 형식

- 이 문서의 색, 타이포 위계, 간격 의미, 컴포넌트 의미는 출력 형식과 독립적인 디자인 계약이다.
- HTML v1만 현재 지원한다. A4와 슬라이드용 페이지 크기, 페이지 나눔, 밀도, 애니메이션 규칙은 각 렌더러가 추가될 때 별도 확장한다.
- 새 출력 형식 때문에 구조화 보고서의 의미나 필드 정의를 HTML 전용으로 변경하지 않는다.

## 14. 자산 및 변경 관리

- 현재 문서는 공식 공개 자료를 분석한 구현 기준이며 LG전자의 전체 내부 브랜드 매뉴얼을 대체하지 않는다.
- 로고, LG EI 폰트, 슬로건 그래픽, 공식 그라디언트를 저장소에 추가하기 전에 사용 권한과 배포 범위를 확인한다.
- 공식 자산을 확보하기 전에는 대체 로고를 만들지 말고 텍스트 기반 제품명만 사용한다.
- 색상, 폰트, 로고, 보고서 구조 또는 오버플로 규칙이 바뀌면 이 문서와 렌더러 검증을 같은 변경에서 갱신한다.
- 공식 LG 브랜드 페이지가 개정되면 원색 값, 자산 사용법, 서체 역할을 다시 확인한다.
